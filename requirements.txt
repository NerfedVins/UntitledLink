# yt-dlp is deliberately unpinned: it breaks whenever a site changes its
# markup, and run.bat refreshes it on every launch. The rest carry floors
# rather than pins - the APIs used here are old and stable, but a machine
# with a five-year-old Pillow should be told rather than fail oddly.
yt-dlp
requests>=2.31
beautifulsoup4>=4.12
pillow>=10.0
